import React from 'react';
import {Container} from "@mui/material";

const HeroSection = () => {
    return (
            <Container>
                <h1>Hero Section</h1>
            </Container>
    );
};

export default HeroSection;
