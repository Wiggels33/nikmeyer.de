'use client'
import React from 'react';
import {Container} from "@mui/material";

const FromSection = () => {
    return (
            <Container>
                <h2>Form Section</h2>
            </Container>
    );
};

export default FromSection;
